<Window x:Class="SmartFocus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SmartFocus" Height="Auto" Width="600" MaxHeight="200"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <SolidColorBrush x:Key="NeonCyan" Color="#00FFFF"/>
        <SolidColorBrush x:Key="BgDark" Color="#0D1117"/>
        <SolidColorBrush x:Key="TextDim" Color="#B0B0B0"/>
        <DropShadowEffect x:Key="GlowEffect" Color="#00FFFF" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
        <DropShadowEffect x:Key="ItemGlow" Color="#00FFFF" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
        <Style x:Key="ResultItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1AFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3300FFFF"/>
                                <Setter TargetName="border" Property="Effect" Value="{StaticResource ItemGlow}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="{StaticResource BgDark}" CornerRadius="12"
            BorderBrush="{StaticResource NeonCyan}" BorderThickness="1.2">
        <Border.Effect>
            <DropShadowEffect Color="#00FFFF" BlurRadius="25" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        <StackPanel Margin="0">
            <TextBox x:Name="SearchBox" FontFamily="Cascadia Code, Consolas, monospace"
                     FontSize="16" Background="Transparent"
                     Foreground="{DynamicResource NeonCyan}" CaretBrush="{DynamicResource NeonCyan}"
                     BorderThickness="0" Padding="16,12"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Rectangle Height="1" Fill="#20FFFFFF" Margin="8,0"/>
            <ListBox x:Name="ResultsList" ItemsSource="{Binding Results}"
                     ItemContainerStyle="{StaticResource ResultItemStyle}"
                     Background="Transparent" Foreground="{StaticResource TextDim}"
                     BorderThickness="0" FontFamily="Cascadia Code, Consolas, monospace"
                     FontSize="14" Padding="4" MaxHeight="300"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     SelectedItem="{Binding SelectedResult}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DisplayName}" Foreground="{StaticResource TextDim}" Margin="0"/>
                            <TextBlock Text="{Binding WindowTitle}" Foreground="#606060" Margin="8,0,0,0" FontSize="12" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</Window>